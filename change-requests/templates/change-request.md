# CR-xxx: Title

Status: Proposed  
Priority: Medium  
Area: Area  
Created: YYYY-MM-DD  
Reviewed: YYYY-MM-DD

## Context

Why this change exists, what prompted it, and what problem it is trying to solve.

Write this from the code, not from memory. State the facts that make the change necessary and name the files that carry them, so a later reader can check whether the context still holds. Update `Reviewed` whenever this section is re-checked against the repository.

## Goal

The desired outcome in plain language.

## Open Questions

Decisions this request cannot be implemented without. Phrase each so it has a decidable answer, and record the answer in `Decisions` rather than editing the question away.

- [ ] Question one.
- [ ] Question two.

Implementation does not start while any box here is unchecked. A request with no open questions is either genuinely trivial or has not been looked at closely enough yet.

## Proposed Implementation

The current implementation approach. Keep this practical and update it as the shape becomes clearer.

Where the work is phased, split it so each phase has a check that can fail — "no visual change, verified by diffing resolved CSS declarations" is a phase; "do the CSS" is not.

## Decisions

One dated entry per resolved question: what was decided, and the fact that decided it. Record the fact even when it contradicts something above — especially then, because the contradiction is the useful part.

## Acceptance Criteria

- [ ] Criterion one.
- [ ] Criterion two.
- [ ] Criterion three.

## Implementation Notes

Capture decisions, tradeoffs, changed assumptions, and relevant file paths as work progresses.

## Outcome

Record what actually changed before marking the request `Done`.
